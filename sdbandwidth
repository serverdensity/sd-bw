#!/usr/bin/env python

"""CLI for checking bandwidth at Server Density.

Usage:
	sdbandwidth devices
	sdbandwidth interfaces
	sdbandwidth bandwidth [(-i <name>) (-d <id>) (-t [(<start> <end>)])]
	sdbandwidth emailme <email>
	sdbandwidth config
	sdbandwidth -h | --help
	sdbandwidth -v | --version

	
Options:
	-d <id>				Use this device instead of current device in config
	-i <name>			Use this network interface instead of the current in config
	-t <start> <end> 	Use this time instead of time in config: YYYY-MM-DDTHH:MM:SSZ
	-v, --version    	Show version
	-h, --help 			Show help
"""

import docopt 

if __name__ == '__main__':
	arguments = docopt.docopt(__doc__, version='SDbandwidth client 0.1')

